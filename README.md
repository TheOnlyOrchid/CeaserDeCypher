This is a simple 'decryption' tool for decoding text encoded in a caeser cipher.

It works by decoding the string with each "key" in turn, scanning for legible words in each decoded string. It outputs any "hits" - when the threshold for legible words is reached.
